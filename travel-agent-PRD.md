# Wayfound — 产品需求文档（PRD）
**版本：** v1.1  
**日期：** 2026-05-17  
**状态：** 待开发

---

## 目录
1. [产品概述](#1-产品概述)
2. [用户场景](#2-用户场景)
3. [功能需求](#3-功能需求)
4. [UI / UX 设计规范](#4-ui--ux-设计规范)
5. [技术架构](#5-技术架构)
6. [Agent 设计](#6-agent-设计)
7. [测试标准](#7-测试标准)
8. [开发优先级](#8-开发优先级)

---

## 1. 产品概述

### 1.1 产品定位
**Wayfound** 是一个**功能性的 AI 旅游行程规划工具**，而非单纯的 AI 对话机器人。用户通过填写出行信息表单触发 AI 生成初版行程，之后可以自由编辑、修改、添加地点，并支持住宿/航班查询与跳转预订。

**产品名：** Wayfound  
**核心 Slogan（待定）：** Find your way, anywhere.

### 1.2 核心价值
- AI 生成个性化行程，替代繁琐的手动搜索
- 可视化双屏联动（行程列表 + 地图），直观验证动线合理性
- AI 主动检测行程问题，降低用户踩坑概率
- 支持多端使用（移动端为主，桌面端为辅）

### 1.3 目标用户
- 自由行旅行者，有一定规划能力但希望提升效率
- 主要设备：手机（iOS / Android）
- 辅助设备：桌面浏览器（用于深度规划）

---

## 2. 用户场景

### 主场景
> 小李计划 5 天 4 夜去京都，预算中等，喜欢历史文化和美食。他打开 Wayfound，在首页表单填入目的地、日期、人数和 Vibe 标签后点击「生成行程」，AI 用 10 秒生成完整五日攻略。他把第 3 天的一个景点换成了朋友推荐的小众神社，在地图上确认动线合理后，导出长图发给同行朋友。

### 次要场景
- 临时加入新地点（"我想去这家网红咖啡馆"）
- 重新规划某一天（"Day 2 太密了，帮我重排"）
- 查询并跳转预订酒店/航班
- 导出攻略为长图分享小红书

---

## 3. 功能需求

### 3.1 信息收集模块（表单 + AI 补充追问）

**主交互方式：一屏表单，用户填写后一键生成行程。**

| 字段 | UI 控件 | 必填 |
|------|---------|------|
| 目的地 | 搜索输入框（带自动补全） | ✅ |
| 出发日期 / 结束日期 | 日历选择器 | ✅ |
| 出行人数 | 数字步进器 + 类型选择（家庭/情侣/朋友/独行） | ✅ |
| 旅行 Vibe | 多选标签（自然 / 人文 / 美食 / 购物 / 休闲 / 探险） | ✅ |
| 预算范围 | 滑动区间选择器（人均/天） | ✅ |
| 特殊需求 | 多选标签（无障碍 / 素食 / 带婴儿 / 宠物友好） | 选填 |

**AI 补充追问（表单提交后）：**
当用户填写的信息存在模糊项时，AI 在生成行程前以**气泡式单问题**追问，例如：
- Vibe 填了「轻松」→ AI 问：「偏向自然风景还是城市咖啡馆风格？」
- 人数 4 人 → AI 问：「有小孩吗？方便我调整行程强度」
- 追问不超过 2 个，用户可跳过直接生成

---

### 3.2 行程数据结构

行程的最小单位为**地点 + 时间段**：

```
行程
└── Day N（第 N 天）
    ├── 上午（09:00 - 12:00）
    │   └── [地点卡片] 景点名 · 类型 · 建议时长 · 评分 · 图片 · 预估费用
    ├── 下午（12:00 - 18:00）
    │   ├── [地点卡片] 午餐餐厅
    │   └── [地点卡片] 景点
    └── 晚上（18:00 - 22:00）
        └── [地点卡片] 晚餐 / 夜间活动
```

---

### 3.3 地点搜索与添加

**方式 A — 搜索列表：**
- 用户输入关键词（如"鼎泰丰 台北"）
- 系统返回候选列表：地点名、图片、评分、地址、类型
- 用户点选确认后加入行程

**方式 B — 地图点选：**
- 用户在地图上长按某位置
- 弹出地点信息卡片，选择"加入行程"并指定时间段

---

### 3.4 行程编辑操作

| 操作 | 交互方式 | 实现阶段 |
|------|----------|----------|
| 移动地点（跨天/跨时段） | 长按拖拽卡片 | 一期 |
| 删除地点 | 左滑删除 / 卡片内删除按钮 | 一期 |
| 调整时间段 | 卡片内下拉选择（上午/下午/晚上） | 一期 |
| 插入新地点 | 时间段内点击"+ 添加" | 一期 |
| 替换地点 | 卡片内"替换"按钮 → 触发搜索 | 二期 |
| 重新生成某天 | Day 标题旁"重新规划"按钮 → AI 重排 | 二期 |

---

### 3.5 AI 主动检测（行程验证）

AI 持续监控行程，发现以下问题时以**非打断式提示条**展示（不弹窗）：

| 规则类型 | 示例提示 |
|----------|----------|
| 行程过密 | ⚠️ Day 2 安排了 6 个地点，建议不超过 4 个 |
| 动线折返 | 🔄 故宫→三里屯→故宫 存在折返，建议调整顺序 |
| 相邻距离过远 | 📍 这两个地点相距 22km，建议中间留交通时间 |
| 餐食缺失 | 🍽️ Day 3 下午没有安排用餐，建议添加午餐 |
| 天气提示 | 🌧️ 出发日期当地雨季，建议减少户外行程 |

用户可以选择忽略或根据提示调整。

**AI 被动响应：** 行程页内嵌对话框，用户可随时追问，如"Day 2 怎么优化？"。

---

### 3.6 住宿 / 航班查询（跳转模式）

- 系统根据行程目的地和日期，查询并展示推荐酒店/航班信息
- 展示内容：名称、价格、评分、图片、链接
- 点击后**跳转至第三方平台**（携程/Booking/Google Flights）完成预订
- 平台自身不处理支付和订单

---

### 3.7 数据存储与导出

**账号体系：**
- 支持 Google / 微信 一键登录
- 行程云端保存，多设备同步
- 支持历史攻略查看

**导出格式（一期实现）：**
- 长图（适合发小红书 / 朋友圈）
- PDF（适合打印/存档）
- 分享链接（只读查看）

**导出格式（二期）：**
- 日历 .ics（导入手机日历）
- 小红书图文格式

---

## 4. UI / UX 设计规范

### 4.1 整体风格

**B + A 混合风格：**

| 页面区域 | 风格 | 说明 |
|----------|------|------|
| 首页 / 目的地探索 | 大图沉浸式（风格 B） | 激发用户「想去」的欲望 |
| 行程规划页 | 清爽卡片式（风格 A） | 提高编辑操作效率 |
| 地点详情页 | 混合（大图 + 信息卡片） | 视觉与信息并重 |

**色调：** 暖色系为主（珊瑚橙 / 暖米白），辅以天空蓝点缀。避免纯工具感的冷灰配色。

---

### 4.2 移动端布局（主）

**行程规划页 — 半屏抽屉模式：**

```
┌─────────────────────────┐
│                         │
│      地图（上半屏）       │  ← 常驻，显示当天地点 pins
│      动态路线连线         │
│                         │
├─────────────────────────┤  ← 可上下拖拽的分割线
│ Day 1  Day 2  Day 3 ... │  ← 天数 Tab
│─────────────────────────│
│ ▌上午                   │
│ [卡片] 景点 A            │  ← 行程卡片列表
│ ▌下午                   │
│ [卡片] 餐厅 B            │
│ [+ 添加地点]             │
│ ▌晚上                   │
│ [卡片] 景点 C            │
└─────────────────────────┘
```

**交互规则：**
- 手指下滑 → 地图扩大，列表缩小至底部条
- 手指上滑 → 列表展开，地图缩至顶部条
- 点击列表卡片 → 地图对应 pin 高亮放大
- 点击地图 pin → 列表滚动至对应卡片

---

### 4.3 地点卡片设计（C 方案 — 丰富版）

```
┌──────────────────────────────┐
│ [封面图 60×60]  景点名称      │
│                ⭐ 4.8  约¥60  │
│                类型 · 建议2h  │
│                           ›  │  ← 点击展开详情
└──────────────────────────────┘
```

展开后显示：
- AI 备注（为什么推荐）
- 开放时间
- 官网/大众点评跳转链接
- AI 提示（如有）

---

### 4.4 桌面端布局（辅）

```
┌──────────────┬──────────────────────────┐
│              │                          │
│  行程列表    │         地图             │
│  （左侧栏）  │      （右侧主区）         │
│              │                          │
│  Day 1       │   [交互式地图]           │
│  ├ 上午      │   [景点 pins]            │
│  ├ 下午      │   [路线连线]             │
│  └ 晚上      │                          │
│              │                          │
└──────────────┴──────────────────────────┘
```

---

## 5. 技术架构

### 5.1 技术栈

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 前端 | React + TypeScript | 组件化，适合复杂交互 |
| 样式 | Tailwind CSS | 快速布局，响应式 |
| 地图（国内） | 高德地图 API | 国内 POI 数据最准，30万次/天免费 |
| 地图（国际） | Mapbox | 颜值高，5万次/月免费 |
| 后端 / 数据库 | Supabase | 免费额度够用，自带 Auth + DB + Storage |
| 部署 | Vercel | 免费，与 React 无缝集成 |
| AI 模型 | DeepSeek API | 主力规划模型 |

### 5.2 地图路由规则

```javascript
const getMapProvider = (destination) => {
  const chinaRegions = ['中国', '北京', '上海', '广州', ...]
  return chinaRegions.includes(destination.country)
    ? 'amap'      // 高德
    : 'mapbox'    // Mapbox
}
```

### 5.3 数据模型（核心）

```typescript
// 行程
interface Itinerary {
  id: string
  userId: string
  destination: string
  startDate: string
  days: Day[]
  createdAt: string
}

// 每天
interface Day {
  dayNumber: number
  slots: TimeSlot[]
}

// 时间段
interface TimeSlot {
  period: 'morning' | 'afternoon' | 'evening'
  places: Place[]
}

// 地点
interface Place {
  id: string
  name: string
  type: 'attraction' | 'restaurant' | 'hotel' | 'shopping'
  coordinates: { lat: number; lng: number }
  rating: number
  estimatedCost: number
  duration: number        // 建议停留（分钟）
  imageUrl: string
  aiNote: string          // AI 推荐理由
  externalUrl: string     // 大众点评/Google Maps 链接
}
```

---

## 6. Agent 设计

### 6.1 架构模式

**Tool-use Agent（工具固定，步骤自决）：**
- 工具集由开发者定义（固定边界）
- DeepSeek 自行决定调用哪些工具、顺序如何、结果如何组合

### 6.2 工具集

| 工具名 | 功能 | 数据源 |
|--------|------|--------|
| `search_places` | 搜索景点/餐厅/POI | 高德 / Mapbox API |
| `get_poi_details` | 获取地点详细信息 | 高德 / Mapbox API |
| `get_weather` | 查询出行期间天气 | OpenWeather API |
| `search_flights` | 查询航班信息 | Amadeus API |
| `search_hotels` | 查询酒店信息 | Booking Affiliate API |
| `validate_itinerary` | 行程合理性检测（Fuzzy） | DeepSeek（LLM-as-Judge） |
| `calculate_distance` | 计算两点间距离/时间 | 高德 / Mapbox API |

### 6.3 行程生成流程

```
用户输入（目的地/天数/人数/取向/预算）
        ↓
Step 1: get_weather → 判断天气，标记不适合户外的日期
        ↓
Step 2: search_places × N → 并行搜索景点、餐厅（按取向筛选）
        ↓
Step 3: calculate_distance → 计算地点间距离，优化动线聚集度
        ↓
Step 4: DeepSeek 规划 → 按天 + 时间段组织地点，考虑体力/预算/取向
        ↓
Step 5: validate_itinerary → Rubric 评分，不达标则自动修正
        ↓
输出结构化行程（JSON）→ 前端渲染
```

---

## 7. 测试标准

### 7.1 测试框架总览

测试分为三个正交维度：

| 维度 | 说明 |
|------|------|
| **E2E vs Component** | 全流程测试 vs 单模块测试 |
| **Objective vs Subjective** | 有标准答案 vs 需要判断的质量 |
| **Quantitative vs Qualitative** | 数字指标 vs 描述性评估 |

---

### 7.2 Deterministic 测试（Component Level）

**工具：Jest（单元）/ Playwright（E2E自动化）**

| 测试项 | 类型 | 验收标准 |
|--------|------|----------|
| 行程天数是否与输入一致 | Objective / Quantitative | 100% 匹配 |
| 每天是否包含早中晚三个时段 | Objective / Quantitative | 覆盖率 ≥ 95% |
| 地点搜索 API 响应时间 | Quantitative | P95 ≤ 2s |
| 行程生成总耗时 | Quantitative | P95 ≤ 10s |
| 地点坐标是否有效（在目的地范围内） | Objective | 错误率 ≤ 1% |
| 编辑操作（拖拽/删除/插入）是否正确响应 | Objective | 100% 通过 |
| 导出文件是否可正常打开 | Objective | 100% 通过 |
| 账号登录/数据同步 | Objective | 成功率 ≥ 99% |

**Playwright E2E 关键路径：**
```
完整用户路径：
输入目的地 → AI 生成行程 → 添加一个地点 → 
拖拽调整顺序 → 查看地图联动 → 导出 PDF
→ 全程无报错，数据一致性验证通过
```

---

### 7.3 Fuzzy 测试（LLM-as-Judge + Rubric）

**工具：DeepSeek 作为裁判模型，评分 1-5 分**

#### 评分 Rubric（7 个维度）

| # | 维度 | 权重 | 1分（差） | 3分（中） | 5分（好） |
|---|------|------|-----------|-----------|-----------|
| ① | **动线合理性** | 高 | 严重折返，需额外2h以上交通 | 有小折返但可接受 | 路线顺畅，相邻地点地理集中 |
| ② | **时间分配** | 高 | 单天超6个地点或严重空档 | 稍密/稍松但可行 | 每天3-4个地点，节奏合理 |
| ③ | **餐食覆盖** | 中 | 全天无餐食安排 | 缺少1顿 | 早中晚均有安排且时间合理 |
| ④ | **体力强度匹配** | 中 | 老人/儿童行程安排强度过高 | 略超出但可调整 | 完全匹配用户类型 |
| ⑤ | **预算匹配** | 中 | 推荐地点费用严重超出预算 | 略超预算10-20% | 推荐费用在预算范围内 |
| ⑥ | **取向匹配** | 高 | 推荐与用户取向完全不符 | 部分匹配 | 景点/餐厅类型高度符合取向 |
| ⑦ | **天气适配** | 低 | 雨季大量安排户外且无备选 | 有少量不适合的安排 | 充分考虑天气，有备选方案 |

**加权总分计算：**
```
总分 = (①×0.20 + ②×0.20 + ③×0.10 + ④×0.10 + ⑤×0.10 + ⑥×0.20 + ⑦×0.10)
验收阈值：总分 ≥ 3.5 / 5.0
```

#### Rubric 评分提示词模板（喂给裁判 DeepSeek）

```
你是一名资深旅游规划师，请按照以下 7 个维度对这份行程进行评分。
每个维度给出 1-5 分（1=差，3=中，5=好），并给出简短理由。
只返回 JSON，不要有其他内容。

用户信息：{user_profile}
行程内容：{itinerary}

返回格式：
{
  "scores": {
    "route_logic": { "score": N, "reason": "..." },
    "time_allocation": { "score": N, "reason": "..." },
    "meal_coverage": { "score": N, "reason": "..." },
    "physical_intensity": { "score": N, "reason": "..." },
    "budget_match": { "score": N, "reason": "..." },
    "preference_match": { "score": N, "reason": "..." },
    "weather_adaptation": { "score": N, "reason": "..." }
  },
  "weighted_total": N,
  "pass": true/false
}
```

#### Fuzzy 测试执行方案

- 每次迭代运行 **50 个测试 case**（覆盖不同目的地/天数/取向组合）
- 记录各维度平均分和通过率
- 通过率目标：**≥ 80%**
- 发现问题的 case 保存为 regression case，下次迭代必须通过

---

### 7.4 Subjective / Qualitative 测试

| 测试项 | 方法 | 频率 |
|--------|------|------|
| 用户满意度（行程质量） | 真实用户评分（1-5星） + 开放反馈 | 每次发布后 |
| UI 易用性 | 用户测试录屏（观察操作路径） | 每个大版本 |
| AI 提示语自然度 | 人工审核 20 条提示文案 | 每次更新提示词 |
| 导出攻略视觉质量 | 人工评审导出样本 | 每次模板更新 |

---

## 8. 开发优先级

### 一期（MVP）
- [ ] 多轮对话收集用户信息
- [ ] AI 生成行程（DeepSeek + 工具调用）
- [ ] 行程列表展示（按天 + 时间段）
- [ ] 地图联动（高亮 + 动线）
- [ ] 移动端半屏抽屉布局
- [ ] 基础编辑：移动、删除、插入、调整时间段
- [ ] 搜索添加地点（搜索列表方式）
- [ ] 账号登录（Google OAuth）+ 云端保存
- [ ] 导出：长图 + PDF

### 二期
- [ ] 地图点选添加地点
- [ ] AI 主动检测 + 提示条
- [ ] 替换地点 / 重新规划某天
- [ ] 航班 / 酒店查询与跳转
- [ ] 被动 AI 对话追问
- [ ] 桌面端响应式适配
- [ ] 导出：分享链接 + .ics 日历

### 三期
- [ ] 社交分享（小红书图文格式）
- [ ] 历史攻略管理
- [ ] 协作编辑（多人共同规划）
- [ ] 离线模式（出行时无网络查看）

---

*文档由需求讨论自动整理生成，如有变更请同步更新版本号。*
